---
description: Git 提交信息版本号格式规范
alwaysApply: true
---

# Git 提交信息格式

所有 git commit 必须使用以下格式：

```
【x.x.x】【实际工作内容】
```

## 版本号规则

当前基线版本：**1.0.0**

| 位 | 含义 | 何时递增 |
| --- | --- | --- |
| 第一位 | 大版本 | 默认为 `1`；仅在站点架构级重构、整体迁移等重大变更时手动升级 |
| 第二位 | 中版本 | 默认为 `0`；变更主题、更换部署方式、站点结构级调整时递增；或第三位达到 `99` 时递增并归零第三位 |
| 第三位 | 小版本 | 每次常规提交递增；从 `0` 起，到 `99` 后进位第二位 |

## 示例

```
【1.0.1】【新增文章：Csharp基础知识】
【1.0.2】【关闭侧边栏今日诗词随机诗句】
【1.1.0】【更换 Volantis 主题配置】
```

## 提交要求

- 方括号内的工作内容用中文，简洁描述本次实际变更
- 一次 commit 对应一类相关改动，不混无关修改
- 创建 commit 前先确认当前应使用的版本号，并在 commit message 中使用正确版本
